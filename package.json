{
  "name": "jarwiz",
  "private": true,
  "version": "0.0.0",
  "description": "Jarwiz — an infinite canvas where live AI agents are your collaborators.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm run dev --workspace apps/web\" \"npm run dev --workspace apps/server\"",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
